# task-manager
Instrucciones rápidas:
1. Instalar Flutter.
2. Copiar `.env.example` a `.env` y ajustar `BASE_URL` si es necesario.
3. Ejecutar `flutter pub get`.
4. Ejecutar `flutter run`.

El proyecto usa:
- provider
- http
- flutter_secure_storage
- flutter_dotenv
